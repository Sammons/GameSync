var server 			= require('./servers/server.js');
var socketServer 	= require('./servers/socket-server.js')
var collision 		= require('collision');
var physics			= require('physics');

collision.initialize();
physics.initialize(collision);

/*
rts
var map = {
	width: 500,
	height: 500,
	sprite: "./public/image.png"
};
var player = {};
player.units = [];
unitCollisionHandler = function(collisionData) {
	...
}

var baseUnit = function(position) {
	this.sprite = { image:"./public/image.png", collisionBody: true},//if false unit needs an explicit collision body
	this.collisionListener = unitCollisionHandler,
	this.health = 50,
	this.movespeed = 25,
}

speed at time
*/

/*
sidescroller

*/

/*
asteroids

*/

/*
lock & shoot

*/